- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.zshrc: zshrc
    ~/.config/nvim/init.vim: init.vim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
